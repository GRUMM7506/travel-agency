name: travel_agency_app
description: Автоматизация туристического агентства — Flutter frontend
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  go_router: ^14.2.7
  dio: ^5.7.0
  intl: ^0.19.0
  fl_chart: ^0.69.0
  flutter_secure_storage: ^11.0.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
